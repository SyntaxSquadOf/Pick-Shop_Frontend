export type Product = {
  id: number;
  name: string;
  price: number;
  stock: number;
  image: string;
};

export type User = {
  id: string;
  name: string;
  lastname: string;
  phone_number: string;
  email: string;
  address: string;
};
